{
  "name": "boundbox",
  "version": "0.1.0",
  "description": "Python/Streamlit visual workspace for drawing labeled boxes over a blank canvas or Wireloom wireframe and handing the layout to an AI as JSON.",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "test": "vitest run && python -m unittest discover -s test -p test_*.py",
    "vendor": "node scripts/vendor-wireloom.mjs",
    "extract": "node scripts/wireloom-extract.mjs",
    "build": "node scripts/build-app.mjs",
    "skill:build": "node scripts/build-skill.mjs",
    "skill:sync": "node scripts/sync-skill.mjs"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "esbuild": "^0.28.1",
    "vitest": "^4.1.10"
  }
}
