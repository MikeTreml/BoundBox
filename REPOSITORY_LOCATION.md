# Canonical repository location

This dashboard's canonical location is `C:\Dashboard\BoundBox`.

The legacy path `C:\Users\miket\Repo\BoundBox` is a Windows directory junction kept for compatibility. New configuration and documentation should use the canonical path.
