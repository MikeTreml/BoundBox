# Dependencies (see note in the PR: node_modules is currently tracked from an
# earlier commit; untrack with `git rm -r --cached node_modules` to make this
# rule take effect for the existing files).
node_modules/

# Exchange folder: session state, snapshots, and AI/user payloads.
# The launcher also writes its own `*` .gitignore inside this directory.
boundbox/

# Knowledge-graph output from the graphify skill.
graphify-out/

# OS / editor noise
.DS_Store
Thumbs.db
